/*LEZIONI 6(14/10) -------------------------------------------------------------------------------------------------------------

VARIABLES ESTATICAS Y AUTOMATICAS---

Cuando una variable es estatica, cuando se cambia de ambito (como cuando se inicia un ciclo o se invoca una funcion)
no se revaloriza la variable, es decir solo se inicializa una vez en todo el flujo de la ejecucion, ademas, mantienen
que solo son visiblen en el ambito que son inicializadas.

VARIABLE REGISTGRO (register type name = ...;)---

La CPU almacena los datos inicializados de esta forma para hacer los calculos de forma mas eficiente sin necesidad de hacer
los viajes de ida y vuelta de la ram, claramente, este registro no es muy grande entonces puede ser de dudosa utilidad, pero mejor que sobre
a no q falte

ARGOMENTI DI DEFAULT

Son valores establecidos en los parametros de la funcion que hacen que no sea necesario llenar todos los argumentos de una funcion al invocarla
.lenght()

RIDEFINIZIONE DI FUNZIONI

Funciones con nombres iguales pero con distintos parametros y probablemente distinta ejecucion

FUNZIONI RICORSIVA

Funciones que se invocan asi mismas

 */
